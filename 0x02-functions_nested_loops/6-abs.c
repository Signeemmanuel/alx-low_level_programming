#include "main.h"

/**
 * _abs - Computes the absolute vlaue of an integer.
 * @n: Number to be computed
 *
 * Return: Abosolute value of @n.
 */
int _abs(int n)
{
	if (n < 0)
		return (-n);
	return (n);
}
