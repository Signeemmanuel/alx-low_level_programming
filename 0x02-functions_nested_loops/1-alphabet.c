#include "main.h"

/**
 * print_alphabet - Print the alphabets in lowercase
 *
 * Return: Always 0 (success)
 */
void print_alphabet(void)
{
	char ch;

	for (ch = 'a'; a <= 'z'; ch++)
		_putchar(ch);
	_putchar('\n');
}
