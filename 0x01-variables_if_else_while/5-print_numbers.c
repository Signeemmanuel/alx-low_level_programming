#include <stdio.h>

/**
 * main - Entry point into Program
 *
 * Return: always return 0 after successful execution of the program
 */
int main(void)
{
	int num;

	for (num = 0; num <= 9; num++)
		printf("%d", num);
	printf("\n");
	return (0);
}


